box: dlang2/ldc-ubuntu:1.15.0

build:
  steps:
    - script:
        name: run tests
        code: |
          apt update && apt install -y git
          git checkout --track origin/gh-pages && git merge master
          cd generator && dub
          git commit -am 'Generate pages'
          git push origin gh-pages
